<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acahualla Primeval Reserve - Sargon</title>
    <style>
        :root {
            /* アークナイツUI風の配色に変更 */
            --bg-dark: #1a1a1a;
            --bg-panel: #2d2d2d;
            --text-main: #e0e0e0;
            --rhodes-blue: #0098dc;
            --alert-orange: #f5a623;
            --sargon-green: #2e4a3d;
            --border-line: #444;
        }

        body {
            /* アークナイツのUIに近いサンセリフ体に変更 */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-dark);
            margin: 0;
            color: var(--text-main);
            line-height: 1.6;
        }

        header {
            background-color: #111;
            color: white;
            padding: 20px;
            text-align: center;
            border-bottom: 4px solid var(--alert-orange);
        }

        .gov-banner {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 10px;
            color: var(--alert-orange);
            display: inline-block;
        }

        header h1 {
            font-size: 1.8rem;
            margin: 10px 0 0 0;
            font-weight: 800;
            letter-spacing: 2px;
        }

        .hero {
            background-color: #000;
            color: white;
            padding: 100px 20px;
            text-align: center;
            /* ※後でアカフラ風のジャングル画像に変更してください */
            background-image: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.8)), url('https://drive.google.com/thumbnail?id=1MAO83-D97JncfT6a-olrg9ocAHbtK1kY&sz=w1200');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            border-bottom: 1px solid var(--border-line);
        }

        .hero p {
            font-size: 1.4rem; 
            font-weight: 300; 
            letter-spacing: 2px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.9); 
            margin: 0;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
        }

        .alert-box {
            background-color: rgba(245, 166, 35, 0.1);
            border: 1px solid var(--alert-orange);
            border-left: 5px solid var(--alert-orange);
            padding: 20px;
            margin: -40px auto 30px;
            position: relative;
            box-shadow: 0 4px 15px rgba(0,0,0,0.5);
            backdrop-filter: blur(5px);
        }

        .alert-box h2 {
            color: var(--alert-orange);
            margin-top: 0;
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* === カルーセル（そのままの色味をアークナイツ風に調整） === */
        .carousel-container {
            width: 100%;
            margin-top: 20px;
            margin-bottom: 40px; 
            position: relative; 
        }
        .carousel {
            display: flex;
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            gap: 15px;
            padding-bottom: 15px;
            scrollbar-width: thin;
            scrollbar-color: var(--alert-orange) var(--bg-dark);
            scroll-behavior: smooth;
        }
        .carousel::-webkit-scrollbar { height: 6px; }
        .carousel::-webkit-scrollbar-track { background: var(--bg-dark); }
        .carousel::-webkit-scrollbar-thumb { background-color: var(--alert-orange); border-radius: 4px; }
        
        .carousel-card {
            scroll-snap-align: center;
            flex: 0 0 85%;
            max-width: 400px;
            background: var(--bg-panel);
            border: 1px solid var(--border-line);
        }
        .carousel-card img { width: 100%; height: 250px; object-fit: cover; display: block; opacity: 0.8; }
        .carousel-card img:hover { opacity: 1; transition: 0.3s; }
        .carousel-card .caption { 
            padding: 12px; font-size: 0.9rem; color: #fff; 
            text-align: left; border-top: 2px solid var(--sargon-green); 
            font-family: monospace; /* 報告書っぽく */
        }

        .carousel-btn {
            position: absolute;
            top: 45%; 
            transform: translateY(-50%);
            background-color: rgba(245, 166, 35, 0.2); 
            color: var(--alert-orange);
            border: 1px solid var(--alert-orange);
            width: 40px;
            height: 40px;
            border-radius: 0; /* 四角にしてUIっぽく */
            font-size: 1.5rem;
            cursor: pointer;
            z-index: 10;
            display: flex; align-items: center; justify-content: center;
            transition: all 0.2s;
        }
        .carousel-btn:hover { background-color: var(--alert-orange); color: #000; }
        .prev-btn { left: -15px; }
        .next-btn { right: -15px; }

        /* === グリッドレイアウト === */
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px; margin-top: 20px; }
        .card { background: var(--bg-panel); padding: 20px; border: 1px solid var(--border-line); border-left: 4px solid var(--sargon-green); }
        .card h3 { color: #fff; border-bottom: 1px solid var(--border-line); padding-bottom: 10px; margin-top: 0; font-weight: normal; }

        .footer { background-color: #0a0a0a; color: #666; text-align: center; padding: 40px 20px; font-size: 0.8rem; margin-top: 60px; border-top: 1px solid var(--border-line); font-family: monospace; }
        .stamp { border: 2px solid var(--alert-orange); color: var(--alert-orange); display: inline-block; padding: 5px 15px; font-weight: bold; margin: 10px 0; letter-spacing: 2px; }

        @media screen and (max-width: 600px) {
            header h1 { font-size: 1.3rem; }
            .hero { padding: 60px 15px; background-attachment: scroll; background-position: center 25%; }
            .hero p { font-size: 1rem; }
            .alert-box { margin-top: -20px; }
            .carousel-card { flex: 0 0 90%; } 
            .carousel-btn { width: 35px; height: 35px; font-size: 1.2rem; }
            .prev-btn { left: 5px; }
            .next-btn { right: 5px; }
        }

        /* アークナイツ風のセクションタイトル */
        h2 { border-left: 5px solid var(--rhodes-blue); padding-left: 15px; font-size: 1.3rem; color: #fff; font-weight: 400; letter-spacing: 1px;}
    </style>
</head>
<body>

<header>
    <div class="gov-banner">Sargon Territorial Administration</div>
    <h1>ACAHUALLA PRIMEVAL RESERVE</h1>
</header>

<div class="hero">
    <p>The impenetrable green heart of Terra.</p>
</div>

<div class="container">
    <div class="alert-box">
        <h2>⚠️ HAZARD WARNING: HIGH ORIGINIUM DENSITY</h2>
        <p>This area contains highly active Originium environments. Entry without a registered Catastrophe Messenger or official Rhodes Island field clearance is strictly <strong>PROHIBITED</strong>. Acute Oripathy risk is severe.</p>
        <div class="stamp">ACCESS RESTRICTED</div>
    </div>

    <section>
        <h2>// Visual Records (Classified)</h2>
        <div class="carousel-container">
            <button class="carousel-btn prev-btn">❮</button>
            <div class="carousel" id="photoCarousel">
                <div class="carousel-card"><img src="https://drive.google.com/thumbnail?id=1xvIqkqIPAKFC1JUuooqiflvsjwCAiwNS&sz=w800"><div class="caption">OBJ-01: Mutated Originium Slug</div></div>
                <div class="carousel-card"><img src="https://drive.google.com/thumbnail?id=1VkYl5CUUkhbWBLQOHWdxQyapIBuY1NH2&sz=w800"><div class="caption">LOC-02: Acahualla Deep River</div></div>
                <div class="carousel-card"><img src="https://drive.google.com/thumbnail?id=1MR9eGLwHAr4Ot1dy2QLrlaHb2mLLCM5S&sz=w800"><div class="caption">PER-03: Tiacauh Tribe Warrior</div></div>
                <div class="carousel-card"><img src="https://drive.google.com/thumbnail?id=1j98fzuj4-JEPG13u1HWPgc23i9nZhu_W&sz=w800"><div class="caption">FAC-04: 'Big Ugly' Relic Site</div></div>
                <div class="carousel-card"><img src="https://drive.google.com/thumbnail?id=1o_pI8UQMfNtnZyTaEaH0Y4992X51EWYr&sz=w800"><div class="caption">LOC-05: Sargon Ancient Ruins</div></div>
            </div>
            <button class="carousel-btn next-btn">❯</button>
        </div>
    </section>

    <section>
        <h2>// Designated Observation Sectors</h2>
        <div class="grid">
            <div class="card">
                <h3>Sector A: Tiacauh Grounds</h3>
                <p><strong>大酋長の試練場近郊：</strong>現在、ティアカウ部族間の「物理的対話（決闘）」が頻発しているため、調査員の立ち入りは推奨されません。</p>
            </div>
            <div class="card">
                <h3>Sector B: Relic Deck</h3>
                <p><strong>『ビッグ・アグリー』残骸観測所：</strong>過去に稼働していた巨大機械の残骸。現在も一部の部族民が改修を試みているため、接近時は注意が必要です。</p>
            </div>
            <div class="card">
                <h3>Sector C: Active Zone</h3>
                <p><strong>源石変異植物セクター：</strong>高濃度の活性源石によって変異した食虫植物の群生地。防護服（クラス4以上）の着用が義務付けられています。</p>
            </div>
        </div>
    </section>
</div>

<div class="footer">
    <p>TERRA YEAR 109X © Acahualla Preservation Committee | Assisted by Rhodes Island Ecological Dept.</p>
    <p>Originium Density: 0.38u/L | Status: HAZARDOUS</p>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const carousel = document.getElementById('photoCarousel');
        const prevBtn = document.querySelector('.prev-btn');
        const nextBtn = document.querySelector('.next-btn');
        function getScrollAmount() {
            const card = document.querySelector('.carousel-card');
            return card ? card.offsetWidth + 15 : 300;
        }
        nextBtn.addEventListener('click', function() {
            const maxScrollLeft = carousel.scrollWidth - carousel.clientWidth;
            if (Math.ceil(carousel.scrollLeft) >= maxScrollLeft - 5) {
                carousel.scrollTo({ left: 0, behavior: 'smooth' });
            } else {
                carousel.scrollBy({ left: getScrollAmount(), behavior: 'smooth' });
            }
        });
        prevBtn.addEventListener('click', function() {
            if (Math.floor(carousel.scrollLeft) <= 5) {
                carousel.scrollTo({ left: carousel.scrollWidth, behavior: 'smooth' });
            } else {
                carousel.scrollBy({ left: -getScrollAmount(), behavior: 'smooth' });
            }
        });
    });
</script>
</body>
</html>
